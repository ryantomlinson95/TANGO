<%= form_with(model: project, local: true) do |form| %>
  <% if project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name  if @project.new_record?%>
    <%= form.text_field :name, id: :project_name if @project.new_record? %>
  </div>

  <div class="field">
    <%= form.label :description if @project.new_record? %>
    <%= form.text_area :description, id: :project_description if @project.new_record? %>
  </div>

  <div class="field">
    <%= form.label :githuburl if @project.new_record? %>
    <%= form.text_field :githuburl, id: :project_githuburl if @project.new_record? %>
  </div>

  <div class="field">
    <%= form.label :progress if @project.new_record? %>
    <%= form.number_field :progress, id: :project_progress if @project.new_record? %>
  </div>

  <div class="field">
    <%= form.label :dev unless @project.new_record? %>
    <%= form.text_field :dev, id: :project_dev unless @project.new_record? %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
